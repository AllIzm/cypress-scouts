{
  "dependencies": {
    "cypress": "^12.13.0"
  },
  "scripts": {
    "cyopen": "npx cypress open",
    "cyrun": "npx cypress run",
    "cyrun:chrome": "npm run cyrun -- --browser chrome"
  },
  "devDependencies": {
    "@cypress/xpath": "^2.0.3"
  }
}
